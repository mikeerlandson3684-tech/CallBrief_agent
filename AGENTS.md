# AGENTS.md

## Cursor Cloud specific instructions

This is a greenfield repository for **CallBrief_agent** — described as "an AI call logger."

### Current State

As of initial setup, the repository contains only a `README.md`. There is no application code, no dependency files, no build system, and no services to run.

### Environment

- **Node.js**: Available via nvm (v22.x)
- **Python**: 3.12.x (system)
- **OS**: Ubuntu (linux)

### Development Notes

- No lint, test, build, or run commands exist yet — they will need to be added alongside the first application code.
- No package manager lockfiles exist; choose one (e.g., `pnpm`, `pip`, `uv`) when adding the first dependencies.
- No database or external service dependencies are configured.
