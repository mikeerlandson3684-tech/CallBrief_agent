# AGENTS.md

## Cursor Cloud specific instructions

This is a **greenfield repository** for **CallBrief_agent** — described as "an AI call logger".

### Current state
- The repo contains only a `README.md` (project name + one-line description).
- There is **no application code, no dependencies, no services, no tests, no CI** as of the initial commit.

### Environment
- No update script is needed yet — there are no dependencies to install.
- Available toolchain on the Cloud Agent VM: Node.js (v22), Python 3.12, Git 2.43.
- Once code is added, this file should be updated with build/run/test instructions.
